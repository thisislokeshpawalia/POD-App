{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "aptPackages": [
      "ffmpeg"
    ],
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
